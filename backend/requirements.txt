fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
python-dotenv==1.0.1

# Added in Phase 6 (Razorpay) and Phase 4 (AI) — listed here now so the
# lockfile story is decided early, per Section L ("decide versions and
# infrastructure early"). Not imported by any Phase 2 code yet.
#
# razorpay 2.0.1 (not the scaffold's 1.4.2): 1.4.2 imports the removed
# `pkg_resources` module at import time and is broken on setuptools >= 81,
# which is what a clean Python 3.12 environment now installs. 2.0.1 drops
# that dependency. Verified importable here.
razorpay==2.0.1
# google-genai (the current unified Google GenAI SDK; NOT the deprecated
# `google-generativeai`) — the primary AI provider. Gemini has a free tier
# (limits set by Google, not guaranteed free). Pinned to 1.75.0: google-genai
# 2.x requires pydantic>=2.12.5, and this project pins pydantic==2.10.3.
google-genai==1.75.0
# groq (official Groq SDK) — OPTIONAL fallback provider only, disabled by
# default (AI_FALLBACK_ENABLED=false). Used solely so the AI buyer/parser can
# survive a transient Gemini outage (HTTP 429/503/504/timeout); Gemini stays
# the default provider. See app/ai/groq_client.py. 1.7.0 declares
# pydantic<3,>=1.9.0 and httpx<1,>=0.23.0 — both satisfied by this file's
# existing pins.
groq==1.7.0

# Testing (Phase 3 onward)
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
