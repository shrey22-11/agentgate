[pytest]
# asyncio_mode=auto lets plain `async def test_*` run without a per-test
# marker. pytest-asyncio is the only async plugin in use.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
addopts = -ra
